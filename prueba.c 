#include <stdio.h>
#include <stdlib.h>
#include "APIG23.h"

Grafo ConstruirGrafo() {

    //Parser 
    char * c = malloc(sizeof(char));
    while(EOF)){
        c=getchar(file); 
    }
    if('c'=='p'){
        
    }
}
